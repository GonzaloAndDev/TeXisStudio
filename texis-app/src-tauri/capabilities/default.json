{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability por defecto — permisos estándar de la app de escritorio.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
